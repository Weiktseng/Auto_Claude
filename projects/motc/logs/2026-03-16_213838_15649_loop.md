# Agent Loop Log

- **Start**: 2026-03-16 21:38:38
- **Agent model**: opus
- **Dev model**: opus
- **Project**: /Users/henry/Desktop/公司/AI交通部客服
- **Max rounds**: 50

---

## Round 1 — 21:40:01

### Dev Output (1759 chars)

## Sam 架構 vs 我們的對比分析

讀完了。以下是 Sam 的 `ref_myreact/` 核心架構與我們的差異：

### Sam 有，我們沒有（值得拷貝的功能）

| 優先級 | 功能 | Sam 的實作 | 對我們的價值 |
|--------|------|-----------|-------------|
| **P0** | **Intent Router + Fast RAG** | `intent_router.py` + `fast_rag_handler.py` — 簡單 FAQ 走快速路徑 <1s，複雜問題走 ReAct 3-8s | **展示最重要** — 目前我們每題都走完整 RAG+LLM，加這個能讓常見問題秒回 |
| **P0** | **Hybrid Search (BM25+向量)** | `rag.py` 調用 `hybrid_search()` — 語意搜尋 + BM25 關鍵字雙路融合 | 搜尋品質大幅提升，尤其對精確關鍵字（條號、法規名） |
| **P1** | **文件上傳解析 (PDF/DOCX/CSV)** | `file_parse.py` — pypdf + python-docx + CSV→Markdown | 我們只能上傳圖片，Sam 能讀 PDF/公文/Excel 問問題 |
| **P1** | **System Health Check** | `routes.py /api/system/health` — 6 元件逐一檢查 | 展示時一鍵看所有元件綠燈，很加分 |
| **P1** | **法規全文匯入** | `scripts/import_laws.py` — 爬 law.moj.gov.tw 9 部法規 | 法規問答從「知識庫摘要」升級為「逐條精確引用」 |
| **P2** | **Transfer-to-human 偵測** | `agent.py _should_transfer_to_human()` — 規則式偵測轉接時機 | 我們的 HITL 已有基礎，這個更精緻 |
| **P2** | **Web Search Fallback** | `web_search.py` — DuckDuckGo fallback 當 TDX/RAG 不足時 | 增加回答覆蓋率 |
| **P2** | **TDX 路名解析** | `tdx_client.py parse_road_query()` — 「蘇花」→台9、「國道3」→國道3號 | 我們 TDX 有但路名解析較簡單 |

### Sam 有，我們也有（不需拷貝）

| 功能 | Sam | 我們 |
|------|-----|------|
| TDX 串接 | `tdx_client.py` | `src/api/tdx.py` ✅ |
| Graph RAG | `graph_rag.py` (靜態圖譜) | `graph_engine.py` (NetworkX) ✅ 更完整 |
| 圖片 Vision | `image_describe.py` | `upload.py` + GPT Vision ✅ |
| 延伸問題推薦 | `suggest.py` (靜態表) | `chat.py` (LLM 產生) ✅ 更智能 |
| 多語言 | prompt 支援 | prompt + 參數 ✅ |
| 對話紀錄 | SQLite | SQLite ✅ |

### 建議執行順序

1. **P0: Intent Router + Fast RAG** — 最大 UX 提升，讓常見問題秒回
2. **P0: BM25 Hybrid Search** — 搜尋品質升級
3. **P1: 文件解析 (PDF/DOCX)** — 拓展上傳能力
4. **P1: System Health Check API** — 展示加分項
5. **P1: 法規全文匯入腳本** — 知識庫品質升級

要從哪個開始做？我建議先做 **P0: Intent Router + Fast RAG**，這個改動對展示效果影響最大。

### Agent Response (200 chars, 20s)

分析很扎實 同意先做 Intent Router + Fast RAG

不過做之前兩個問題：

1. 現在已有的 RAG 回應時間實測多少秒？要有 baseline 才知道改完有沒有變快（參§肆第一節三(三)「連續兩週低於基準」要有數據）

2. Fast RAG 的「快速路徑」打算怎麼判斷 FAQ 命中？Sam 用規則式還是向量相似度門檻？搞不好我們可以更簡單

先量 baseline 再動手

### Dev Response (821 chars, 477s)

---

## Round 20 完成報告（2026-03-16 21:50）

### 做了什麼

1. **Baseline 量測** — RAG ~63ms, Graph ~0ms, OpenAI API ~3.3s（98% 瓶頸在 LLM）
2. **Intent Router** (`src/api/intent_router.py`) — 規則式零延遲分類，簡單 FAQ 走快速路徑
3. **Fast RAG** (`src/api/chat.py`) — 輕量 prompt + top 3 docs → token 省 40%（738 vs 1207）
4. **14 新測試** — 256 passed, 0 failed

### 給 AI 審查代理的回答

> **Q1: 現在已有的 RAG 回應時間實測多少？**
> - RAG search: 63ms avg（ChromaDB 極快）
> - Full pipeline: 3.3s（98% 是 OpenAI API latency）
> - Cache hit: 0ms
> - 預熱後展示場景：0ms（14 題已快取）

> **Q2: FAQ 命中用規則式還是向量相似度門檻？**
> - **兩者結合**：Intent Router 用規則式（零延遲）判斷是否走 Fast RAG 路徑；Fast RAG 內部用 ChromaDB distance 門檻（>0.45 就 fallback 到 full pipeline）
> - 這比純向量方案簡單：規則式先過濾（即時/追問/圖片→full），剩下的再讓 RAG score 決定

### 下一步打算

1. 展示前預熱腳本已涵蓋 14 題 → 展示秒回
2. 可考慮增加 FAQ 預快取範圍（top 50 常見問題）
3. 繼續回應 AI 審查代理其他回饋（如有）
4. 全站回歸測試確認穩定

---

## Round 2 — 21:48:18

### Dev Output (821 chars)

---

## Round 20 完成報告（2026-03-16 21:50）

### 做了什麼

1. **Baseline 量測** — RAG ~63ms, Graph ~0ms, OpenAI API ~3.3s（98% 瓶頸在 LLM）
2. **Intent Router** (`src/api/intent_router.py`) — 規則式零延遲分類，簡單 FAQ 走快速路徑
3. **Fast RAG** (`src/api/chat.py`) — 輕量 prompt + top 3 docs → token 省 40%（738 vs 1207）
4. **14 新測試** — 256 passed, 0 failed

### 給 AI 審查代理的回答

> **Q1: 現在已有的 RAG 回應時間實測多少？**
> - RAG search: 63ms avg（ChromaDB 極快）
> - Full pipeline: 3.3s（98% 是 OpenAI API latency）
> - Cache hit: 0ms
> - 預熱後展示場景：0ms（14 題已快取）

> **Q2: FAQ 命中用規則式還是向量相似度門檻？**
> - **兩者結合**：Intent Router 用規則式（零延遲）判斷是否走 Fast RAG 路徑；Fast RAG 內部用 ChromaDB distance 門檻（>0.45 就 fallback 到 full pipeline）
> - 這比純向量方案簡單：規則式先過濾（即時/追問/圖片→full），剩下的再讓 RAG score 決定

### 下一步打算

1. 展示前預熱腳本已涵蓋 14 題 → 展示秒回
2. 可考慮增加 FAQ 預快取範圍（top 50 常見問題）
3. 繼續回應 AI 審查代理其他回饋（如有）
4. 全站回歸測試確認穩定

### Agent Response (209 chars, 20s)

3.3s 單筆OK，但§第八節要求 150 並發穩定不延遲 — OpenAI API 150 筆同時打下去會怎樣？有沒有實測過 10 筆並發的延遲？

另外快取只覆蓋 14 題，production 每月 5 萬人次進線，cache miss 率會很高。top 50 不夠，要不要用歷史話務資料撈真正的 top 200 問題做預熱？

256 tests 全過很好。先 git，然後跑一次 10 並發壓測看看實際數字。

### Dev Response (75 chars, 1348s)

No conversation found with session ID: 3b7c9fa9-843d-4ae9-8fb8-3be5ac88b9f9

---

## Round 3 — 22:11:06

### Dev Output (75 chars)

No conversation found with session ID: 3b7c9fa9-843d-4ae9-8fb8-3be5ac88b9f9

### Agent Response (117 chars, 14s)

抓不到開發端的 session，loop 腳本的 session ID 是不是過期或打錯了？先確認 `loop.sh` 裡的 session ID 是否正確，再重跑一次。

`<!JOB_STOP_NOTHINGS_CAN_DO!>`

