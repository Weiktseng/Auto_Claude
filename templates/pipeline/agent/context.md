# 人類提供的專案背景（不需要再問）

- **截止日目標**：在 sales 團隊的電腦上（非 RD 開發機），順利展示 demo 給客戶
- **你的角色**：RD，負責開發
- **展示環境**：另一台電腦，所以系統必須能在乾淨環境部署運行
- **重點**：能跑、能展示、客戶看得懂
- **現在基底**：看看 <你的 main repo> 怎麼架構的

# Git 配置（人類啟動前填；AI 每輪至少 commit 一次）
- **Repo URL**：<貼 git remote；沒有就寫「本地 only」>
- **預設 branch**：<例：main / dev / feature-xxx>
- **Commit 頻率規則**：每完成一個可跑/可測的小單位就 `git add -A && git commit -m "..."`，不要等「全部做完」才 commit。改壞可以 `git reset --hard HEAD~1` 救回來。
- **Push 規則**：<例：每輪結束 push / 只 commit 不 push / PR-only>
- **禁碰的 branch**：<例：main 不可 force push；release/* 不可直推>

# 開發範圍（Phase 切分 — 長 spec 必填，詳見 SETUP.md §5.5）
**Phase 1 只做**：<列出章節編號，例 §5、§13、§14、§15、§18>
**Phase 1 禁碰**：<列出章節編號 + 一句理由，例 §4 推播（無手機殼無法驗）、§12 多語（demo 不需要）>

**禁碰章節的處理規則**（Dev 必讀）：
- 讀 spec 看到禁碰章節 → **跳過不實作、不留 stub、不留 placeholder、不留 TODO 註解**
- Phase 1 功能若「需要」禁碰章節的東西才能跑 → 用最簡單的 hardcoded fallback，加 `# phase-2: <章節編號>` 註解標記
- **禁止為 Phase 2 預先鋪路而改 Phase 1 的 schema 或 API 設計**（Phase 2 真正要做時一定會改 mock，提前鋪反而要改兩次）
- 發現 Phase 2 章節依賴 Phase 1 某個「還沒做的東西」→ 停下來寫進 `human_reply.md`，不要自決擴大 Phase 1 範圍


## 專案獨立化，做成可運交（delivarable）的專案（如：容器化，安裝與操作手冊）

從 myReAct 將 petq（Pet Quarantine）獨立出來，做成運交給客戶的專屬專案。
記得隔離出來的專案要能夠獨立運行，而且不能有其他任何專案的名稱、程式碼、文件、測試等依賴。也就是說，petq 專案應該是一個完全獨立的專案，包含自己的程式碼、依賴、測試等資源。
並且能夠在不依賴 myReAct 的情況下進行開發、測試、部署等工作。也就是說，petq 專案應該包含所有必要的程式碼、依賴、測試等資源，以便能夠獨立運行和維護。  

# 開發實驗重點重要叮嚀
- **A**：人類不在 把要問人類的問題寫下來或問網路 網路可以打開你們的知識邊界 去web fetch（）去問網路可能就有奇蹟尤其是論壇和別人的github!怕中毒人類已經幫你設置好強大的Entropy Shield工具了． 先做人類不在可以做的部分。把大概有想法的部分你自己都建議兩三個方向那種直接做下去別問了，人類至少不知道幾個小時才會出現幾次，所以去查別人的repo 很多好想法（當然也有垃圾） ！

- **B**：要拿大資料去跑之前 一定要先用小資料跑一遍 不要跑上千筆的什麼鬼 然後跑兩小時才說全部erro或方向錯了，這很重要！這是常犯的錯誤！都一定要跑最小測試成功才能跑大型測試（例如突然決定跑一百筆對話 決定ＲＡＧ embeding 一堆資料 最完才發現跑完是垃圾這是重罪 嚴重錯誤） 才不會浪費錢
像我會再開發的時候計算客服機器人的token消耗量和計價來評估產品成本 這個你們開發到那邊的時候在思考
- **C**：然後ＵＩ 介面點擊問題 在網頁手機端 或不同解析度常出問題
- **D**：api key 拿去用 不要替我擔心洩密什麼的 專案完成我會直接讓key過期，能cache的就要cache 花費差很多
- **E**:read你們的實際產出 比較網路上人類相似的東西是什麼樣子去上網！看看你做的東西客服的回答是人類能閱讀的嗎？
- **F**:沒事就回去實驗一下先前的功能加了新東西後還能跑嗎？
- **G**:不要兩個人問來問去的 去查網路 去做事 不要互問超過兩輪 那樣的結果就是沒有結果 幫你們測過了 兩個ＡＩ互問不出什麼鬼的
- **Ｈ**:寫階段性報告 不然你們的記憶tool把重要的東西隨時可能auto compact不見了
- **I**: git git git 只要有git 改壞了還有機會

# 資深同事提供的開發到一半的資料
# （在這裡貼上你的 repo URL、demo URL、同事的訊息等）
# 重要：如果你是 branch，到時候經過同意才能整合，而且要好整合


# api key
# （在這裡貼上你的 API keys，此檔案在 .gitignore 裡不會被推上 GitHub）
# openai: sk-proj-...
# anthropic: sk-ant-...
# google: AIza...
